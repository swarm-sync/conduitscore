# Syndication drafts — “Google rank vs AI visibility”

Each file is the **same article** formatted for one destination’s rules. Update the canonical URL below once the post is live on ConduitScore.

| File | Platform |
|------|----------|
| `medium.md` | Medium — `#` title + kicker + canonical footer (`***` ruler) |
| `dev-to.md` | DEV / dev.to — YAML front matter per [Editor Guide](https://dev.to/p/editor_guide) (`tags` comma-separated, **max 4**) |
| `hashnode.md` | Hashnode — YAML per [Publish for GitHub / frontmatter](https://docs.hashnode.com/) (`tags` comma-separated, ≤5 slugs; `canonical` for originals) |
| `linkedin-article.md` | LinkedIn Article — headline in composer + body (**no duplicate H1**) |
| `indie-hackers.md` | Indie Hackers — **`##`** top-level headings (single `#` is reserved for IH title UI) |
| `reddit-crosspost.md` | Reddit text post (**shortened TL;DR** + link to canonical — check subreddit self-promotion rules) |
| `hacker-noon.md` | Hacker Noon Story editor meta block + Markdown body |
| `substack.md` | Substack post / Notes — `#` headline; paywall divider notes in HTML comment (`<!-- paywall -->` if you fork paywalled) |

**Canonical (original) URL placeholder:** `https://conduitscore.com/blog/google-rank-ai-invisibility`

If you publish elsewhere first, flip canonical on each platform so ConduitScore stays the SEO source.
