<p>Hi {{ greeting_name }},</p>
<p>{{ benchmark_note }}</p>
<p>The gap usually tracks to <strong>{{ top_issue }}</strong> — top performers in your bucket have that cleaned up.</p>
<p>Full context: <a href="{{ verify_link }}">open your scan entry link</a> · <a href="{{ rescan_link }}">re-run free</a>.</p>
{% include "partials/signature_ben.html.j2" %}
